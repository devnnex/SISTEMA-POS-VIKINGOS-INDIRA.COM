{
  "name": "vikingos-indira-pos",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite app",
    "build": "vite build app --outDir ../dist-app --emptyOutDir",
    "site:build": "node scripts/build-site.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run test && npm run build && npm run site:build",
    "tauri": "tauri",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "@tauri-apps/plugin-dialog": "^2.4.0",
    "@tauri-apps/plugin-sql": "^2.3.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0",
    "vite": "^7.1.0"
  }
}
